#!/bin/bash

# .: this calls a command from an executable file
# ~/: is the home directory of the current user
# .bashrc: is an executable file containing terminal aliases.
## We are going to execute this command to 'reinitialize' out collection of system wide aliases.
## Aliases must be added to the end of the file before this command is run for changes to take effect.

. ~/.bashrc
